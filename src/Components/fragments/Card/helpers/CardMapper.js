import React from 'react'
import styled from 'styled-components'
import { Card, Image, Section, Text, Title } from '../index'

const CardMapper = () => {
  return <div></div>
}

export default CardMapper
