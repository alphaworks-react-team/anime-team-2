import React, { useEffect } from 'react'
import { categoriesModel, RequestOptions } from '../utils/index'

const { General: CategoriesRequest } = RequestOptions

const Categories = () => {
  useEffect(() => {
    ;(async () => {})()
  }, [])
  return <div></div>
}

export default Categories
